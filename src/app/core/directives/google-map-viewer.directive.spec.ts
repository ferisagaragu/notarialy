import { GoogleMapViewerDirective } from './google-map-viewer.directive';

describe('GoogleMapViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleMapViewerDirective();
    expect(directive).toBeTruthy();
  });
});

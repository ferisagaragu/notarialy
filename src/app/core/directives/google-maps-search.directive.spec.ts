import { GoogleMapsSearchDirective } from './google-maps-search.directive';

describe('GoogleMapsSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleMapsSearchDirective();
    expect(directive).toBeTruthy();
  });
});
